name: "CodeQL Advanced Security"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 2 * * 0' # Esegue una scansione profonda automatica ogni domenica alle 2:30 di notte

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        # ATTENZIONE: Lascia qui SOLO i linguaggi che sono effettivamente presenti nel tuo codice sorgente.
        # Se non hai file JS/TS scritti da te, rimuovi 'javascript' e lascia solo ['python']
        language: [ 'python', 'javascript' ]

    steps:
    - name: Checkout del codice
      uses: actions/checkout@v4

    - name: Inizializza CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        # queries: security-extended,security-and-quality

    - name: Autobuild
      uses: github/codeql-action/autobuild@v3

    - name: Esegui l'Analisi CodeQL
      # Rimosso il parametro 'with: category' perché nella v3 è gestito automaticamente in modo sicuro
      uses: github/codeql-action/analyze@v3